class User {
  id;
  firstName;
  lastName;
  avatar;

  constructor(id, firstName, lastName, avatar) {
    this.id = id;
    this.lastName = lastName;
    this.firstName = firstName;
    this.avatar = avatar;
  }

  
}
